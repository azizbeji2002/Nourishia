<form method="post" action="{{ path('app_dossiers_medicaux_delete', {'id': dossiers_medicaux.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dossiers_medicaux.id) }}">
    <button class="btn-del ">Delete</button>
</form>
